services:
  - type: web
    name: igreja-backend
    env: node
    plan: free
    buildCommand: echo "No build needed"
    startCommand: node server-http.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
